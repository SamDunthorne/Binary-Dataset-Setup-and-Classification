# GPU TensorFlow on WSL2 (Linux). On a recent NVIDIA GPU, install with:
#   pip install "tensorflow[and-cuda]"
tensorflow[and-cuda]>=2.18
numpy
Pillow          # PIL: tiling (tile_images.py)
opencv-python   # cv2: augmentation (augment_images.py)
